#!/usr/bin/env bash

# +--------+
# | System |
# +--------+

# alias shutdown='sudo shutdown now'

# +----+
# | ls |
# +----+

alias ls='ls --color=auto'
alias l='ls -l'
alias ll='ls -lh'
alias la='ls -lhA'
# alias lls='ls -lahFtr'

# +-----+
# | Git |
# +-----+

alias gs='git status'
# alias gss='git status -s'
alias ga='git add'
alias gp='git push'
alias gb='git branch '
alias gc='git commit'
alias gd='git diff'
